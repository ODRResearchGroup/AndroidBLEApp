// Configuration file for InfluxDB settings
// In production, you might want to use a more secure method for storing credentials

// This is an unfilled example of what should be in config.ts
export const CONFIG = {
  INFLUX_TOKEN: '',
  INFLUX_URL: 'http://localhost:8086',
  INFLUX_ORG: 'my-org',
  INFLUX_BUCKET: 'sensor-data',
};